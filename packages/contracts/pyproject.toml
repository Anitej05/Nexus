[project]
name = "nexus-contracts"
version = "1.0.0"
description = "NEXUS platform contracts"
requires-python = ">=3.11,<3.12"
dependencies = [
    "pydantic>=2.13.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
