[project]
name = "nexus-storage"
version = "0.1.0"
description = "Tenant-isolated storage and secret adapters for NEXUS"
requires-python = ">=3.11,<3.12"
dependencies = ["nexus-contracts"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
nexus-contracts = { workspace = true }
